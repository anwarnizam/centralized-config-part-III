message: This service shows all the payment information
orderValue: 100

# Activate actuator refresh endpoint
management:
  endpoints:
    web:
      exposure:
        include: refresh,health
